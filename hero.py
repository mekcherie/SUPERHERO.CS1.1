class Hero:
    # we want our hero to have a default "starting_health"
    def__init__(self, name, starting_health=100)
    self.name= name 
    self.starting_health = starting_health
    self.current_health = starting_health